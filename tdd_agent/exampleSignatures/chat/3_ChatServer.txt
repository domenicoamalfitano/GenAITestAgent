#constructor for the class ChatServer with fields:
# int serverPort and Map<String, ClientHandler> utentiConnessi ----- nickname -> client connesso
ChatServer(int port)
# start the server, accepts new connections and create a ClientHandler for each client.
void start()
# register a new user if the nickname is not already used.
synchronized boolean registraUtente(String nickname, ClientHandler handler)
# remove the user when he log out.
synchronized void deregistraUtente(String nickname)
# send a private message from sender to recipient.
synchronized void inviaMessaggioPrivato(String mittente, String destinatario, String testo)