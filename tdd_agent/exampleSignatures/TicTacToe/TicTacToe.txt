# initializes an empty 3x3 board and sets the starting player
TicTacToeGame()
# attempts to place the player's symbol (X or O) at the specified row and column; returns true if the move is valid.
boolean makeMove(int row, int col)
# checks if the game has been won by either player; returns the winning symbol ('X', 'O') or '-' if no win.
char checkWinner()
# returns true if the board is full and there is no winner.
boolean isDraw()
# resets the board to start a new game
void reset()